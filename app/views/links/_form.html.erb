<%= form_with(model: link, local: true) do |form| %>
  <% if link.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(link.errors.count, "error") %> prohibited this link from being saved:</h2>

      <ul>
      <% link.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label "The url you would like the shortened url to go to" %>
    <%= form.text_area :out_url, id: :link_out_url %>
  </div>


  <div class="actions">
    <%= form.submit "Generate shortened url" %>
  </div>
<% end %>
